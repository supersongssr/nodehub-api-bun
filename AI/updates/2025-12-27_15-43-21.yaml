when: "2025-12-27T15:43:21+08:00"
where:
  - config/config.toml
  - src/utils/config.ts
  - QUICKSTART.md

why: |
  配置文件需要在 config.toml 中清晰展示所有需要的变量和参数。
  使用 $VARIABLE 占位符可以：
  1. 在 config.toml 中一目了然地看到所有配置项（包括敏感项）
  2. 明确标注哪些变量来自 .env 文件（因为 .env 不上传 git）
  3. 提供统一的配置视图，避免在两个文件之间来回查看

how: |
  1. 更新 config/config.toml：
     - 所有配置项都在 config.toml 中声明
     - 敏感值使用 $VARIABLE 占位符格式
     - 添加 [security] 段，包含 api_key = "$API_KEY"
     - [panels] 段包含完整的 URL 和 API_KEY 配置
     - [dns] 段包含 provider, zone_id, cloudflare_api_token
     - [notifications] 段包含 telegram_bot_token, telegram_chat_id

  2. 更新 src/utils/config.ts：
     - 添加 replaceEnvPlaceholders() 函数
     - 支持 $VAR_NAME 和 ${VAR_NAME} 两种格式
     - 递归处理配置对象中的所有字符串
     - 自动替换占位符为环境变量值
     - 如果环境变量未设置，返回空字符串
     - 更新所有配置接口以包含完整字段

  3. 更新 QUICKSTART.md：
     - 添加 "Configuration Syntax" 部分
     - 说明 $VARIABLE 占位符语法
     - 提供示例代码展示用法
     - 说明占位符的替换来源（.env 和系统环境变量）

must:
  - ✓ config.toml 包含所有配置项（包括敏感项）
  - ✓ 敏感值使用 $VARIABLE 占位符
  - ✓ 占位符正确替换为环境变量值
  - ✓ 配置加载测试通过
  - ✓ API_KEY 正确读取（显示 ***sZCg）
  - ✓ SSP_URL 正确读取（显示完整 URL）
  - ✓ CLOUDFLARE_API_TOKEN 正确读取（显示 ***PndT）
  - ✓ TELEGRAM_TOKEN 正确设置
  - ✓ QUICKSTART.md 更新配置语法说明
